import { VersaoMaisCompletaHighlightDirective } from './versao-mais-completa-highlight.directive';

describe('VersaoMaisCompletaHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new VersaoMaisCompletaHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
